import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  quote: {
    padding: 40,
    paddingBottom: 10,
    textAlign: 'center',
    fontSize: 20,
  },
  text: {
    fontSize: 18,
  },
});

export default styles;
